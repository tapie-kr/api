generator client {
  provider        = "prisma-client-js"
  output          = "../../client"
  previewFeatures = ["omitApi", "prismaSchemaFolder"]
  // seed = "../../seed.ts"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
